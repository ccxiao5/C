
	因数分解是十分基本的数学运算，应用广泛。下面的程序对整数n(n>1)进行因数分解。比如，n=60, 则输出：2 2 3 5。请补充缺失的部分。

void f(int n)
{
	for(int i=2; i<n/2; i++)
	{
		____________________
		{
			printf("%d ", i);
			n = n / i;
		}
	}
	if(n>1) printf("%d\n", n);
}
